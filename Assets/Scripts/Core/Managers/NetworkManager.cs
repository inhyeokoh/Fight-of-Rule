using ServerCore;
using System.Net;
using System.Net.Sockets;

/*public class NetworkManager : SubClass<GameManager>
{
    protected override void _Init()
    {
        //초기화(연결설정 등등)
        //서버 ip or 도메인 설정
        //IPAddress ipaddr = IPAddress.Parse("도메인 or ip");
        //IPEndPoint ipendpoint = new IPEndPoint(ipaddr, 7000);  //ipendpoint로 자동으로 ipv4 패밀리로 지정됨
        //Connector connector = new Connector();                  //연결 설정만 그대로 받아서 연결
        // GameManager.ThreadPool.EnQueueJob(() => { connector.Connect(ipendpoint, () => { return _session; }); });
    }
    protected override void _Excute()
    {
      
        
        //Update와 같이 매 프레임 해야하는 작업
    }

    protected override void _Clear()
    {
        //자원 정리
    }
}
*/